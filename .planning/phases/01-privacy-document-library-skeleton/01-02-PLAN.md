---
phase: 01-privacy-document-library-skeleton
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/io/shubham0204/smollmandroid/ui/screens/documents/DocumentLibraryActivity.kt
  - app/src/main/java/io/shubham0204/smollmandroid/ui/screens/documents/DocumentLibraryViewModel.kt
  - app/src/main/java/io/shubham0204/smollmandroid/ui/screens/documents/DocumentDetailsScreen.kt
  - app/src/main/java/io/shubham0204/smollmandroid/ui/components/AppConfirmDialog.kt
  - app/src/main/res/values/strings.xml
autonomous: true
requirements:
  - PRIV-02
  - PDF-01
  - PDF-02
  - PDF-03
  - UX-02
user_setup: []
must_haves:
  truths:
    - User can import PDFs via picker and via share-to-app.
    - Library list shows filename, import date, and page count (when available) sorted newest-first.
    - User can delete with confirmation from library list and from a document details screen.
    - Privacy/offline messaging is explicit and does not imply cloud upload/storage.
  artifacts:
    - app/src/main/java/io/shubham0204/smollmandroid/ui/screens/documents/DocumentLibraryActivity.kt
    - app/src/main/java/io/shubham0204/smollmandroid/ui/screens/documents/DocumentDetailsScreen.kt
  key_links:
    - DocumentLibraryViewModel uses DocumentRepository import/list/delete
    - Manifest supports receiving `application/pdf` via share-to-app
---

<objective>
Add Phase 1 UI for the PDF document library: import, list, delete, privacy/offline messaging.

Purpose: Deliver the user-facing library baseline to enable later extraction/indexing.
Output: A library screen + details screen + manifest share-to-app support + clear errors.
</objective>

<execution_context>
@F:/2026/3_SMOLCHAT/SmolChat-Android/.codex/get-shit-done/workflows/execute-plan.md
@F:/2026/3_SMOLCHAT/SmolChat-Android/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-privacy-document-library-skeleton/01-CONTEXT.md

Existing patterns:
@app/src/main/java/io/shubham0204/smollmandroid/ui/screens/model_download/ImportModelScreen.kt
@app/src/main/java/io/shubham0204/smollmandroid/ui/screens/chat/ChatActivity.kt
@app/src/main/AndroidManifest.xml
</context>

<tasks>

<task type="auto">
  <name>Task 01: Add document library Activity + ViewModel + screen</name>
  <files>app/src/main/java/io/shubham0204/smollmandroid/ui/screens/documents/DocumentLibraryActivity.kt, app/src/main/java/io/shubham0204/smollmandroid/ui/screens/documents/DocumentLibraryViewModel.kt</files>
  <read_first>app/src/main/java/io/shubham0204/smollmandroid/ui/screens/chat/ChatActivity.kt, app/src/main/java/io/shubham0204/smollmandroid/ui/screens/model_download/ImportModelScreen.kt, .planning/phases/01-privacy-document-library-skeleton/01-CONTEXT.md</read_first>
  <action>
Create a new library entry point that:
- Shows a list of imported PDFs with fields: filename, import date, and page count (if available).
- Default sort: newest imported first.
- Includes a delete action per row.
- Includes an Import button that launches a PDF picker (SAF) and calls the repository import.
- Shows explicit privacy/offline messaging text: documents stay on-device; no cloud upload/storage.
- Shows clear user-facing errors on import failure (corrupt/unsupported PDFs).

Do NOT show placeholder indexing state in Phase 1.
  </action>
  <verify>rg -n "DocumentLibrary" app/src/main/java/io/shubham0204/smollmandroid/ui/screens/documents/DocumentLibraryActivity.kt</verify>
  <acceptance_criteria>
    - app/src/main/java/io/shubham0204/smollmandroid/ui/screens/documents/DocumentLibraryActivity.kt exists
    - app/src/main/java/io/shubham0204/smollmandroid/ui/screens/documents/DocumentLibraryViewModel.kt exists
    - Library UI contains visible text communicating on-device/offline behavior
  </acceptance_criteria>
  <done>Library screen imports + lists docs and surfaces errors clearly</done>
</task>

<task type="auto">
  <name>Task 02: Add document details screen with delete (citation/source UI landing)</name>
  <files>app/src/main/java/io/shubham0204/smollmandroid/ui/screens/documents/DocumentDetailsScreen.kt</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-privacy-document-library-skeleton/01-CONTEXT.md</read_first>
  <action>
Add a minimal document details screen reachable from the library list. This is the Phase 1 “source/citation-related document UI” landing place.

Requirements:
- Shows the document’s filename and metadata.
- Includes a Delete button that uses the same confirmation + repository hard delete path.
  </action>
  <verify>rg -n "DocumentDetails" app/src/main/java/io/shubham0204/smollmandroid/ui/screens/documents/DocumentDetailsScreen.kt</verify>
  <acceptance_criteria>
    - Document details screen exists and includes a delete action
  </acceptance_criteria>
  <done>User can delete from a document UI that will later be used for sources/citations</done>
</task>

<task type="auto">
  <name>Task 03: Add confirmation dialog component used for deletes</name>
  <files>app/src/main/java/io/shubham0204/smollmandroid/ui/components/AppConfirmDialog.kt</files>
  <read_first>app/src/main/java/io/shubham0204/smollmandroid/ui/components/AppAlertDialog.kt</read_first>
  <action>
Create a reusable confirmation dialog for delete actions.

Requirements:
- Used by both library list delete and document details delete.
- No undo required.
  </action>
  <verify>rg -n "Confirm" app/src/main/java/io/shubham0204/smollmandroid/ui/components/AppConfirmDialog.kt</verify>
  <acceptance_criteria>
    - Confirmation dialog component exists and is referenced from both delete entry points
  </acceptance_criteria>
  <done>Delete always requires confirmation</done>
</task>

<task type="auto">
  <name>Task 04: Add share-to-app support for PDFs</name>
  <files>app/src/main/AndroidManifest.xml</files>
  <read_first>app/src/main/AndroidManifest.xml, app/src/main/java/io/shubham0204/smollmandroid/ui/screens/chat/ChatActivity.kt</read_first>
  <action>
Add an intent-filter for `ACTION_SEND` with `application/pdf` so users can share a PDF to the app.

Routing behavior:
- The share target should open the document library flow and import the provided PDF.
- If import fails, show a clear error.

Do not remove the existing `text/plain` share intent.
  </action>
  <verify>rg -n "application/pdf" app/src/main/AndroidManifest.xml</verify>
  <acceptance_criteria>
    - AndroidManifest.xml contains an intent-filter that accepts `application/pdf`
    - The existing `text/plain` share intent remains present
  </acceptance_criteria>
  <done>Users can share a PDF to the app to import it</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./gradlew :app:testDebugUnitTest` passes
- [ ] Manual smoke: import via picker + share-to-app, then delete from both library list and document details
</verification>

<success_criteria>

- Library list UX matches decisions (fields + sorting + delete)
- Deletion uses confirmation and hard delete semantics
- Privacy/offline copy is explicit and does not imply cloud upload
- Corrupt/unsupported PDFs show a clear error
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-document-library-skeleton/01-02-SUMMARY.md`
</output>


