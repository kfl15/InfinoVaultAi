---
phase: 01-privacy-document-library-skeleton
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/io/shubham0204/smollmandroid/data/DocumentsDB.kt
  - app/src/main/java/io/shubham0204/smollmandroid/data/AppDB.kt
  - app/src/main/java/io/shubham0204/smollmandroid/data/DocumentRepository.kt
  - app/src/main/java/io/shubham0204/smollmandroid/data/DerivedDataPurger.kt
autonomous: true
requirements:
  - PRIV-01
  - PDF-01
  - PDF-02
  - PDF-03
  - UX-02
user_setup: []
must_haves:
  truths:
    - Imported PDFs are copied into app-private storage and remain available offline.
    - Deleting a PDF removes the stored file and removes its record from the local DB.
    - Deleting a PDF calls a single purge hook that will later delete chunks/embeddings/derived index data.
  artifacts:
    - app/src/main/java/io/shubham0204/smollmandroid/data/DocumentsDB.kt
    - app/src/main/java/io/shubham0204/smollmandroid/data/DocumentRepository.kt
    - app/src/main/java/io/shubham0204/smollmandroid/data/DerivedDataPurger.kt
  key_links:
    - DocumentRepository.deleteDocument(...) deletes file + DB row + invokes DerivedDataPurger.purgeForDocument(...)
---

<objective>
Create the Phase 1 data + storage backbone for a local PDF library.

Purpose: Enable reliable offline document import/list/delete before any indexing work.
Output: Room table + repository APIs for import/list/delete with a hard-delete purge hook.
</objective>

<execution_context>
@F:/2026/3_SMOLCHAT/SmolChat-Android/.codex/get-shit-done/workflows/execute-plan.md
@F:/2026/3_SMOLCHAT/SmolChat-Android/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-privacy-document-library-skeleton/01-CONTEXT.md
@.planning/phases/01-privacy-document-library-skeleton/01-RESEARCH.md

Relevant existing patterns:
@app/src/main/java/io/shubham0204/smollmandroid/ui/screens/model_download/ImportModelScreen.kt
@app/src/main/java/io/shubham0204/smollmandroid/data/AppDB.kt
</context>

<tasks>

<task type="auto">
  <name>Task 01: Add Room model + DAO for stored PDF documents</name>
  <files>app/src/main/java/io/shubham0204/smollmandroid/data/DocumentsDB.kt, app/src/main/java/io/shubham0204/smollmandroid/data/AppDB.kt</files>
  <read_first>app/src/main/java/io/shubham0204/smollmandroid/data/AppDB.kt, app/src/main/java/io/shubham0204/smollmandroid/data/ModelsDB.kt</read_first>
  <action>
Create a new Room entity + DAO for library PDFs.

Requirements:
- Entity fields (minimum):
  - `id` (String UUID)
  - `displayName` (String)
  - `importedAtEpochMs` (Long)
  - `storedPath` (String)
  - `pageCount` (Int?)
  - `fileSizeBytes` (Long)
- DAO supports:
  - List all docs ordered by `importedAtEpochMs DESC`
  - Get by id
  - Insert
  - Delete by id

Update `AppDB.kt` to include the new DAO.
  </action>
  <verify>rg -n "@Entity\(" app/src/main/java/io/shubham0204/smollmandroid/data/DocumentsDB.kt</verify>
  <acceptance_criteria>
    - app/src/main/java/io/shubham0204/smollmandroid/data/DocumentsDB.kt exists
    - app/src/main/java/io/shubham0204/smollmandroid/data/DocumentsDB.kt contains "importedAtEpochMs"
    - app/src/main/java/io/shubham0204/smollmandroid/data/DocumentsDB.kt contains "ORDER BY importedAtEpochMs DESC"
    - app/src/main/java/io/shubham0204/smollmandroid/data/AppDB.kt references the new DAO
  </acceptance_criteria>
  <done>Room table + DAO exist and can list docs newest-first</done>
</task>

<task type="auto">
  <name>Task 02: Add DerivedDataPurger hook for hard delete semantics</name>
  <files>app/src/main/java/io/shubham0204/smollmandroid/data/DerivedDataPurger.kt</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-privacy-document-library-skeleton/01-CONTEXT.md</read_first>
  <action>
Create a tiny interface (or functional type) that represents purging derived data for a document.

Required API:
- `fun purgeForDocument(documentId: String)`

Phase 1 implementation can be a no-op default, but the hook must exist so deletion later also removes chunks/embeddings/derived index data without changing call sites.
  </action>
  <verify>rg -n "interface DerivedDataPurger" app/src/main/java/io/shubham0204/smollmandroid/data/DerivedDataPurger.kt</verify>
  <acceptance_criteria>
    - app/src/main/java/io/shubham0204/smollmandroid/data/DerivedDataPurger.kt contains "purgeForDocument"
  </acceptance_criteria>
  <done>Hard-delete purge hook exists as a single integration point</done>
</task>

<task type="auto">
  <name>Task 03: Implement DocumentRepository import/list/delete (file copy + offline persistence)</name>
  <files>app/src/main/java/io/shubham0204/smollmandroid/data/DocumentRepository.kt</files>
  <read_first>app/src/main/java/io/shubham0204/smollmandroid/ui/screens/model_download/ImportModelScreen.kt, app/src/main/java/io/shubham0204/smollmandroid/data/DocumentsDB.kt</read_first>
  <action>
Implement a repository that:
- Imports a PDF from a `Uri` by streaming bytes into app-private storage.
  - Store files under a dedicated subdirectory (e.g., `filesDir/documents/`).
  - Use a stable generated filename (UUID + ".pdf"); keep `displayName` separately.
- Best-effort compute `pageCount` (nullable) and capture `fileSizeBytes`.
- Exposes a Flow/list API returning newest-first.
- Deletes a document by:
  1) deleting the stored PDF file (if present)
  2) deleting DB row
  3) calling `DerivedDataPurger.purgeForDocument(documentId)`

Corrupt/unsupported PDFs:
- If open/copy fails, do not create a DB row and return a typed failure that UI can present as a clear error message.
  </action>
  <verify>rg -n "class DocumentRepository" app/src/main/java/io/shubham0204/smollmandroid/data/DocumentRepository.kt</verify>
  <acceptance_criteria>
    - app/src/main/java/io/shubham0204/smollmandroid/data/DocumentRepository.kt contains "filesDir"
    - app/src/main/java/io/shubham0204/smollmandroid/data/DocumentRepository.kt contains "purgeForDocument"
    - app/src/main/java/io/shubham0204/smollmandroid/data/DocumentRepository.kt contains "delete" and "storedPath"
  </acceptance_criteria>
  <done>Repository supports offline import/list/delete with hard-delete hook</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./gradlew :app:testDebugUnitTest` passes
- [ ] `./gradlew :app:lintDebug` passes (if enabled)
</verification>

<success_criteria>

- Data model and repository exist and are callable from UI
- Delete path has a single purge hook for derived data
- No crashes on import failures (typed error returned)
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-document-library-skeleton/01-01-SUMMARY.md`
</output>

